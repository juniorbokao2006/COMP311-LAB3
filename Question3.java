public class Question3 {
    public static void main(String[] args) {
        int number = 2; // First even number in the range

        // Increment by 2 to target only even integers up to 50
        while (number <= 50) {
            System.out.println(number);
            number += 2;
        }
    }
}
